# Fusionaly Configuration Example
# Copy this to .env and customize for your environment

# =============================================================================
# Application Settings
# =============================================================================
FUSIONALY_APP_NAME=fusionaly
FUSIONALY_APP_PORT=3000
FUSIONALY_ENV=development  # Options: development, production, test
FUSIONALY_LOG_LEVEL=debug  # Options: debug, info, warn, error
FUSIONALY_PRIVATE_KEY=88888888888888888888888888888888  # Change in production!
FUSIONALY_SESSION_TIMEOUT_SECONDS=1800
FUSIONALY_DOMAIN=localhost:3000

# =============================================================================
# Database Settings
# =============================================================================
FUSIONALY_DB_TYPE=sqlite
FUSIONALY_STORAGE_PATH=storage

# Database Connection Pool
# Auto-detected based on FUSIONALY_ENV if not set:
# - development/test: MaxOpenConns=1, MaxIdleConns=1 (required for E2E tests)
# - production: MaxOpenConns=10, MaxIdleConns=5 (better read concurrency)
# 
# Override defaults by uncommenting:
# FUSIONALY_DB_MAX_OPEN_CONNS=10
# FUSIONALY_DB_MAX_IDLE_CONNS=5

# =============================================================================
# File Paths
# =============================================================================
FUSIONALY_PUBLIC_DIR=web/dist
FUSIONALY_PUBLIC_ASSETS_URL_PREFIX=/

# =============================================================================
# GeoIP Database (Optional)
# =============================================================================
# GeoLite2 database for country/city detection. Optional - works without it.
#
# To enable GeoIP:
# 1. Register at https://www.maxmind.com/en/geolite2/signup
# 2. Download GeoLite2-City.mmdb from your MaxMind account
# 3. Set the path below (or mount in Docker)
#
# Docker: -v /path/to/GeoLite2-City.mmdb:/app/internal-storage/GeoLite2-City.mmdb
#
FUSIONALY_GEO_DB_PATH=internal-storage/GeoLite2-City.mmdb

# =============================================================================
# Logging Settings
# =============================================================================
FUSIONALY_LOGS_DIR=logs
FUSIONALY_LOGS_MAX_SIZE_IN_MB=20
FUSIONALY_LOGS_MAX_BACKUPS=10
FUSIONALY_LOGS_MAX_AGE_IN_DAYS=30

# =============================================================================
# Job Scheduling
# =============================================================================
FUSIONALY_JOB_INTERVAL_SECONDS=60

# =============================================================================
# Production-Specific Settings
# =============================================================================
# When running in production (FUSIONALY_ENV=production):
# - Set a strong FUSIONALY_PRIVATE_KEY (32+ random characters)
# - Configure FUSIONALY_DOMAIN to your actual domain
# - Consider increasing DB connection pool for higher load:
#   FUSIONALY_DB_MAX_OPEN_CONNS=20
#   FUSIONALY_DB_MAX_IDLE_CONNS=10
# - Set appropriate FUSIONALY_LOG_LEVEL (info or warn)
